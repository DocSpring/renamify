---
title: Installation
description: How to install Refaktor
---

## Quick Install

The easiest way to install Refaktor on Linux or macOS:

```bash
curl -fsSL https://docspring.github.io/refaktor/install.sh | bash
```

Or if you need sudo:

```bash
curl -fsSL https://docspring.github.io/refaktor/install.sh | sudo bash
```

To install without sudo to `~/.local/bin`:

```bash
curl -fsSL https://docspring.github.io/refaktor/install.sh | bash -s -- --local
```

The install script will:
- Detect your operating system and architecture
- Download the appropriate binary
- Install it to `/usr/local/bin` (or `~/.local/bin` with `--local`)
- Verify the installation

## Manual Installation

### Linux

#### x86_64 / amd64
```bash
curl -L https://github.com/DocSpring/refaktor/releases/latest/download/refaktor-linux-amd64.tar.gz | tar xz
sudo mv refaktor /usr/local/bin/refaktor
sudo chmod 755 /usr/local/bin/refaktor
```

#### arm64
```bash
curl -L https://github.com/DocSpring/refaktor/releases/latest/download/refaktor-linux-arm64.tar.gz | tar xz
sudo mv refaktor /usr/local/bin/refaktor
sudo chmod 755 /usr/local/bin/refaktor
```

### macOS

#### Intel (x86_64)
```bash
curl -L https://github.com/DocSpring/refaktor/releases/latest/download/refaktor-macos-amd64.tar.gz | tar xz
sudo mv refaktor /usr/local/bin/refaktor
sudo chmod 755 /usr/local/bin/refaktor
```

#### Apple Silicon (arm64)
```bash
curl -L https://github.com/DocSpring/refaktor/releases/latest/download/refaktor-macos-arm64.tar.gz | tar xz
sudo mv refaktor /usr/local/bin/refaktor
sudo chmod 755 /usr/local/bin/refaktor
```

### Windows

*Coming soon - Windows builds will be available in a future release*

## Install Script

*Coming soon*

```bash
curl -fsSL https://install.refaktor.dev | sh
```

## Package Managers

*Coming soon*

```bash
# Cargo
cargo install refaktor

# Chocolatey (Windows)
choco install refaktor

# Arch Linux
yay -S refaktor
```

## Build from Source

For developers who want to build from source:

**Prerequisites**: Rust 1.89.0 or later

```bash
git clone https://github.com/DocSpring/refaktor
cd refaktor
cargo build --release
cp ./target/release/refaktor /usr/local/bin/
```

## Verify Installation

```bash
refaktor --help
```

You should see the help output showing available commands and options.
