---
title: Refaktor
description: Smart search & replace for code and files with case-aware transformations
---

import { Card, CardGrid, LinkCard } from '@astrojs/starlight/components';

<Card>
<div style="font-size: 1.35rem;">
Smart search & replace for code and files with <br/>**case-aware transformations** and **built-in undo/redo**.
<p>Available as a **CLI**, **MCP Server for AI Agents**, and **VS Code Extension**.</p>
</div>
</Card>

## 🤖 MCP Server for AI Agents

<LinkCard
  title="Use Refaktor with AI Assistants"
  href="/refaktor/mcp/overview"
  description="Connect Refaktor to Claude, Cursor, and other AI tools via the Model Context Protocol (MCP) for intelligent, automated refactoring."
/>

The Refaktor MCP Server enables AI agents to:
- **Plan refactorings** with case-aware transformations
- **Preview changes** in AI-friendly formats
- **Apply changes atomically** with full undo/redo support
- **Manage refactoring history** across your codebase

<CardGrid>
	<Card title="Case-Aware" icon="approve-check">
		Automatically converts between snake_case, camelCase, PascalCase, kebab-case, and more.
	</Card>
	<Card title="File Renaming" icon="document">
		Renames files and directories that match your patterns, not just content.
	</Card>
	<Card title="Safety First" icon="seti:lock">
		Plan/apply workflow with atomic operations, backups, and unlimited undo/redo.
	</Card>
	<Card title="Cross-Platform" icon="laptop">
		Works consistently on Linux, macOS, and Windows with platform-specific optimizations.
	</Card>
</CardGrid>

## What is Refaktor?

Refaktor is a command-line tool that understands different naming conventions and can rename both file contents and the files themselves in a single atomic operation. It's designed for developers who need to refactor codebases safely and efficiently.

Traditional find-and-replace tools work at the text level, but don't understand code conventions. If you want to rename `getUserName` to `fetchUserProfile`, you need to handle:

- `getUserName` (camelCase)
- `get_user_name` (snake_case) 
- `GetUserName` (PascalCase)
- `GET_USER_NAME` (SCREAMING_SNAKE_CASE)
- Files like `get-user-name.js`
- Directories like `user-name/`

Refaktor handles all of these automatically in a single operation.

## Quick Example

Rename `getUserName` to `fetchUserProfile` across your entire codebase:

```bash
refaktor rename getUserName fetchUserProfile
```

This automatically handles:
- `getUserName` → `fetchUserProfile`
- `get_user_name` → `fetch_user_profile`  
- `GetUserName` → `FetchUserProfile`
- `GET_USER_NAME` → `FETCH_USER_PROFILE`
- Files like `getUserName.js` → `fetchUserProfile.js`

You can also specify specific paths:
```bash
refaktor rename getUserName fetchUserProfile src/ tests/
```

## Demo

<LinkCard
  title="See Refaktor in Action"
  href="/refaktor/demo"
  description="Watch Refaktor rename itself, then use the renamed binary to change it back."
/>

## Build Status

[![CI](https://github.com/DocSpring/refaktor/actions/workflows/ci.yml/badge.svg)](https://github.com/DocSpring/refaktor/actions/workflows/ci.yml)

[![E2E Tests](https://github.com/DocSpring/refaktor/actions/workflows/e2e.yml/badge.svg)](https://github.com/DocSpring/refaktor/actions/workflows/e2e.yml)
